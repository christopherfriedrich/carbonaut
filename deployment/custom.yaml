nginx:
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: carbonaut.cloud
        paths:
          - path: /
            pathType: Prefix
    tls:
      # empty, disabled.